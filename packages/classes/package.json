{
  "name": "@stupidassistant/classes",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
		"test": "echo \"Unit testing is not configured for package, skipping.\""
  },
  "peerDependencies": {
    "react": ">=17"
  },
  "files": [
    "dist",
    "src",
    "!dist/tsconfig.tsbuildinfo"
  ],
  "license": "MIT",
  "repository": "git@github.com:wixplosives/sample-monorepo.git",
  "sideEffects": false,
  "private": true,
  "publishConfig": {
    "access": "public"
  },
  "jest": {
    "preset": "ts-jest",
    "testPathIgnorePatterns": [
      "dist"
    ]
  },
  "dependencies": {
    "vm2": "^3.9.5"
  },
  "devDependencies": {
    "@stupidassistant/types": "^1.0.0",
    "@types/events": "^3.0.0"
  }
}
